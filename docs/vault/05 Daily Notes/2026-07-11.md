---
tags:
  - daily
  - belegchat
date: 2026-07-11
source: claude
chat_url: unbekannt
---

# 2026-07-11

## BelegChat #belegchat

> [!success] Alpha E2E grün — [[BelegChat - PMO HUB]]

### Ergebnis

- Beleg **`01-2026-0004`** (Turmventilator Philips, 135 €, 19.06.2026) erfolgreich erfasst
- Mehrseiten-Flow: Foto → Foto → **`Fertig`** → OCR + `belege` + Abschluss *BERENT1*
- Dialog-Rückfragen über *BERENT2* (E2E), Abschlussnachricht im *BERENT1*-Chat

### Fixes (Session)

| Problem | Fix |
|---------|-----|
| HTTP 400 „Threema Ungültig“ | Dialog via Edge `action: send` (E2E), nicht `send_simple` |
| Endlosschleife bei leeren Callbacks | Leere `box` → Stille ignorieren |
| Abgeschnittene Dialogtexte | PKCS7-Padding in Edge Function (`padThreemaInnerMessage`) |
| Kein Abschluss-Push | `Bestätigung senden` → *BERENT1* per `send_simple` |
| JSON Body not valid JSON | `Beleg-Seiten speichern`: Raw-JSON-Objekt statt Array (n8n 2.x) |
| `Fertig` nicht erkannt | PKCS7-Strip + Typ-Bytes `0x01`/`0x02`, `startsWith('fertig')` |

Patch-Skripte: `n8n-workflows/scripts/fix-*.mjs` (9 Stück), Workflow live per n8n API.

### Entscheidungen

- [[Decisions/ADR-02 Mehrseiten-Fertig-UX]] — ersetzt ADR-01 (Ziffern 1/2)
- Zwei Threema-Gateways: Dialog *BERENT2*, Abschluss *BERENT1*

## Phase 1 GoBD-Härtung (BER-92) #belegchat

> [!success] Phase 1 abgeschlossen — DB, Edge und n8n live, E2E-Testbeleg `01-2026-0005` grün (2-seitig, echte Upload-Zeitstempel, 2× `seite_archiviert`)

- Migration `post_alpha_gobd_hardening` (20260711075401) angewendet: `archived_at`, Duplikat-Unique, Hash-Format-Checks, Festschreibungs-Trigger, `audit_log` append-only, RLS auf `pending_belege`/`beleg_seiten`
- Edge Function v16: `archive-beleg-seite` liefert `archivedAt`
- n8n-Export angepasst (`Pending Body`, `Beleg-Seiten speichern`, `Audit-Log schreiben` → `seite_archiviert` pro Seite) — **Import in Live-n8n ausstehend** (kein `N8N_API_KEY` lokal)
- 12 DB-Tests grün (Rollback-Transaktion) · Doku: `belegchat/docs/GOBD.md` + [[Decisions/ADR-03 GoBD-Härtung DB]]
- Alpha-Migration `alpha_multipage_gobd` nachträglich im Repo versioniert

## BER-95 Früher Push #belegchat

> [!info] Threema-Rückfrage kommt jetzt ~2–6 s früher — direkt nach Integritätsprüfung (NaCl-MAC + Magic Bytes), Archivierung läuft danach

- Edge v17: `decrypt-blob` prüft JPEG/PNG-Magic-Bytes, liefert `detectedMime`; beschädigte Fotos → Fehlerpfad
- RPC `append_pending_seite` (Migration `20260711103000`): atomarer Append, `seite_nr` serverseitig — kein Foto-Verlust bei schnellen Folgefotos
- n8n umverdrahtet: Push nach `Blob entschlüsseln` („Seite N erhalten…"); Fehlerpfad „Seite-Fehler melden" sendet Korrektur, falls Archivierung nach dem Push scheitert
- Live per API aktualisiert + verifiziert · PRs: threema-decrypt#3, n8n-workflows#12
- **E2E-Test grün** (`01-2026-0007`): Push kam früher, `archived_at` echt (40 s/22 s vor Insert), Audit vollständig, kein offener Pending

### Offen (Post-Alpha)

- [x] Post-Alpha-Plan abgelegt (`docs/POST-ALPHA-PLAN.md`, Vault, Linear BER-92/93)
- [x] Phase 1 GoBD-Härtung (BER-92) — *Rest: n8n-Import + E2E-Testbeleg*
- [ ] Phase 2 PDF-Batch CLI (BER-90)
- [ ] Phase 3 Dashboard Auth (BER-93)
- [ ] Vault → Second Brain Sync
