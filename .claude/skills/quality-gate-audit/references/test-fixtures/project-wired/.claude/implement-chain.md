---
provenance:
  origin: ai-claude
  classification: open
  status: reviewed
---

# Implement-Kette (Fixture-Beleg fuer Coverage-Registrierung)

Der /implement-Skill ruft das Coverage-Gate in Schritt 6a-quart auf:

    bash .claude/hooks/coverage-check.sh

Damit ist `coverage-check.sh` in der Implement-Kette registriert (nicht nur vorhanden).
In einem echten INTENTRON-Projekt steht diese Referenz in der implement/SKILL.md-Doku
bzw. im Pre-Commit-/Self-Healing-Pfad — hier als Fixture-Stub abgebildet.
