# BER-122: Mehrere MwSt-Sätze pro Beleg

> **Pre-Flight:** Lies `CLAUDE.md`, dann `docs/UEBERGABE.md`, dann
> `docs/audits/2026-07-23-strukturpruefung-ausbaustufe.md`.
> **Einordnung:** Nächstes Release. Die Konzeptions-Stufe (Nachweis, dass BER-117
> die Erweiterung nicht ausschließt) ist erledigt — s. „Additivitäts-Nachweis".
> **Umsetzung läuft gestaffelt** in der unten festgelegten Reihenfolge
> Migration → App → n8n; siehe „Umsetzungs-Stufen".

## Umsetzungs-Stufen

| Stufe | Inhalt | Stand |
|---|---|---|
| **1** | Datenmodell: Tabelle `beleg_steuerzeilen`, Konsistenz-Trigger (deferred), Festschreibungs-Trigger, RLS, Verhaltenstests, `docs/SCHEMA.md` | **gebaut, 08.08.2026** |
| 2 | App: Steuerzeilen-Editor, Freigabe-Route mit Summenvalidierung, Detailseite | offen (Folge-Story) |
| 3 | DATEV-Export `belegRow → belegRows`, Export-Routen mit JOIN, `docs/DATEV.md` | offen (Folge-Story) |
| 4 | n8n: KI-Schema `steuerzeilen[]`, INSERT via Service Role | offen (Folge-Story) |
| 5 | `docs/UEBERGABE.md`: v1-Beschränkung entfernen | offen — erst wenn Stufe 2–4 stehen; bis dahin ist die Beschränkung noch wahr |

**Warum gestaffelt:** Stufe 1 verankert die riskanteste Entscheidung — das Schema
festgeschriebener Buchungsdaten — bevor App und Export darauf aufsetzen. Bis
Stufe 2 schreibt niemand Zeilen; das Systemverhalten ist unverändert, jeder Beleg
läuft im Ein-Satz-Modus weiter.

### Abweichungen der Umsetzung von diesem Entwurf (Stufe 1)

1. **Konsistenz-Trigger ist `DEFERRABLE INITIALLY DEFERRED`.** Der Entwurf sagt
   nur „Konsistenz-Trigger". Ein gewöhnlicher Row-Trigger kann die Invarianten
   nicht ausdrücken: beim Einfügen von zwei Zeilen existiert nach der ersten
   Zeile genau 1 Zeile (verboten) und die Summe stimmt noch nicht — jeder legale
   Mehrsatz-Beleg würde blockiert. Als CONSTRAINT TRIGGER feuert die Prüfung am
   Transaktionsende gegen den Endzustand.
2. **Zweiter Konsistenz-Trigger auf `belege`.** Ein Trigger nur auf der
   Satellitentabelle feuert nicht, wenn `belege.mwst_satz` gesetzt wird, während
   Zeilen existieren — die Ausschlussregel wäre umgehbar. `bu_schluessel` ist
   nach BER-119 auch nach der Festschreibung einmalig setzbar und fällt ebenfalls
   darunter.
3. **`created_at` ergänzt** (im Entwurf nicht vorgesehen) — konsistent zu
   `beleg_seiten`, hilft bei der Nachvollziehbarkeit.
4. **Festschreibung strenger als `beleg_seiten`:** dort erlaubt BER-118 ein
   INSERT nach der Festschreibung (Dokument nachreichen). Steuerzeilen tragen
   Buchungsbeträge — nach der Festschreibung gibt es keinen zulässigen
   Schreibvorgang mehr, also sind INSERT, UPDATE und DELETE gesperrt.
5. **Bestätigt statt angenommen:** `belege.mwst_satz` ist nullable (Schema-Abfrage
   08.08.2026) — der Mehrsatz-Modus braucht keine Schema-Änderung an `belege`.

## Kontext

Restaurant- und Supermarktbelege führen 7 % und 19 % nebeneinander. Heute trägt
`belege` genau einen Satz (`mwst_satz`, `mwst_betrag`, `betrag_netto`), BER-108
leitet Teilbeträge aus dem einen Satz ab, der Export schreibt eine Zeile pro Beleg,
und BER-117 setzt genau einen `bu_schluessel` pro Beleg. Die n8n-Plausibilität
(BER-110) prüft bewusst nur `netto + mwst = brutto` und verträgt Mischsätze bereits.

## Anforderung

Ein Beleg kann n Steuerzeilen tragen (Satz, Netto, MwSt, Schlüssel je Zeile); der
DATEV-Export schreibt dann n Buchungssätze. Die Erweiterung ist additiv: kein
festgeschriebener Beleg wird verändert, Ein-Satz-Belege funktionieren unverändert.

## Entschiedenes Design

### Datenmodell (Migration dieser Story — Entwurf, bei Umsetzung ins Schwesterrepo)

```sql
CREATE TABLE public.beleg_steuerzeilen (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  beleg_id      uuid NOT NULL REFERENCES public.belege(id) ON DELETE CASCADE,
  pos           smallint NOT NULL CHECK (pos > 0),
  mwst_satz     numeric(5,2) NOT NULL,
  betrag_netto  numeric(12,2) NOT NULL,
  mwst_betrag   numeric(12,2) NOT NULL,
  betrag_brutto numeric(12,2) GENERATED ALWAYS AS (betrag_netto + mwst_betrag) STORED,
  bu_schluessel varchar(4) CHECK (bu_schluessel IS NULL OR bu_schluessel ~ '^[0-9]{1,4}$'),
  UNIQUE (beleg_id, pos)
);
```

Semantik:
- **0 Zeilen** = Ein-Satz-Beleg wie heute (`belege.mwst_satz`/`bu_schluessel` gelten).
  Der GESAMTE Bestand und alle künftigen Ein-Satz-Belege bleiben in diesem Modus —
  es findet KEINE Rückmigration statt.
- **≥ 2 Zeilen** = Mehrsatz-Beleg: die Zeilen sind die Wahrheit; `belege.mwst_satz`
  und `belege.bu_schluessel` sind dann NULL (Konsistenz-Trigger erzwingt:
  Steuerzeilen und Beleg-Einzelsatz schließen sich aus; Summe der Zeilen ==
  `betrag_netto`/`mwst_betrag`/`betrag_brutto` des Belegs auf den Cent).
  Genau 1 Zeile ist verboten (dann Ein-Satz-Modus verwenden) — Konsistenz-Trigger.
- Festschreibung: eigener Trigger auf `beleg_steuerzeilen` nach dem Muster
  `fn_beleg_seiten_unveraenderbar` — INSERT/UPDATE/DELETE nur solange der Beleg
  NICHT festgeschrieben ist. (Die `belege`-Whitelist bleibt unberührt — der
  Satellit hat seinen eigenen Schutz; genau dieses Muster hat die Whitelist-
  Entscheidung aus der Strukturprüfung §2.1 vorgesehen.)

### Erfassung

- Eingang (n8n/OCR): KI-Schema um optionales Array `steuerzeilen[{satz, netto,
  mwst}]` erweitern; liefert die OCR nur Summen, entsteht ein Ein-Satz-Beleg wie
  heute (kein Zwang zur Zeilenbildung). n8n-INSERT der Zeilen via Service Role.
- Freigabe-Formular: Abschnitt „Steuerzeilen" (nur sichtbar, wenn Zeilen existieren
  oder der Nutzer „Beleg hat mehrere Steuersätze" aktiviert): editierbare Zeilen
  (Satz aus {0,5,7,16,19}, Netto, MwSt), Live-Summenprüfung gegen den
  Dokumentbetrag, Schlüssel je Zeile vorbelegt nach BER-117-Gate
  (`vorsteuer_relevant` des Sachkontos + `steuerschluessel`-Mapping je Zeilensatz).
- Freigabe-Route: validiert Summen (±0,01 € Toleranz wie BER-110-Muster: exakte
  Centgleichheit gefordert, Fehler 422 `"Steuerzeilen summieren nicht auf den
  Belegbetrag"`), persistiert Zeilen VOR dem Status-UPDATE.

### Wechselwirkungen (verbindlich entschieden)

- **BER-108 Teilbetrag:** Teilbetrag und Mehrsatz schließen sich in v2 aus —
  Freigabe mit beidem → 422 `"Teilbetrag bei mehreren Steuersätzen noch nicht
  unterstützt"`. (Ein Teil-Split über mehrere Sätze braucht eine Zuordnung, welcher
  Satzanteil gebucht wird — eigene Folge-Story, Feld `gebucht`-Pendant je Zeile ist
  im Modell vorgesehen, aber nicht gebaut.)
- **BER-118:** Manuelle Erfassung bleibt Ein-Satz (Formular ohne Zeilen-Editor in
  v2-Erstausbau; Zeilen können vor der Freigabe im Freigabe-Formular ergänzt werden).
- **DATEV-Export:** `belegRow(b, meta)` → `belegRows(b, meta): string[]` — bei
  0 Steuerzeilen exakt die heutige eine Zeile; bei n Zeilen n Zeilen mit: Umsatz =
  Zeilen-Brutto, gleiches Konto/Gegenkonto/Belegdatum, **Belegfeld 1 identisch**
  (`beleg_nr` — DATEV gruppiert die Sätze über Belegfeld 1 zum Beleg),
  BU-Schlüssel der Zeile in Spalte 9, Buchungstext + Suffix ` (<satz> %)`.
  `anzahl_belege` in `datev_exporte` zählt weiterhin Belege; neue Spalte wird NICHT
  eingeführt (Zeilenzahl ergibt sich aus der Datei).
- **BER-121:** unverändert gültig — Fassungen speichern Bytes, egal wie viele
  Zeilen ein Beleg erzeugt.

### Additivitäts-Nachweis (warum BER-117 nichts zementiert)

Kein festgeschriebener Beleg muss angefasst werden: Bestandsbelege bleiben im
Ein-Satz-Modus dauerhaft gültig; der Mehrsatz-Modus entsteht ausschließlich für NEUE,
noch offene Belege über eine Satellitentabelle mit eigenem Festschreibungs-Trigger;
der Export wechselt auf Code-Ebene von „eine Zeile pro Beleg" zu „Zeilen pro Beleg".
Einzige Vorleistungen in BER-117 (bereits umgesetzt): Schlüssel-Zuordnung liegt als
Konfigurationstabelle mit `mwst_satz`-Schlüssel vor (je Zeile wiederverwendbar), und
die Whitelist-Trigger-Architektur lässt Satellitentabellen zu, ohne die
`belege`-Sperrliste zu ändern.

## Akzeptanzkriterien

**Stufe 1 (diese Umsetzung):**

- [x] Tabelle + Konsistenz-Trigger: 1 Zeile → Fehler (T1); 2 Zeilen mit Summen ≠
      Belegbeträge → Fehler (T2); 2 korrekte Zeilen + `belege.mwst_satz IS NOT NULL`
      → Fehler (T3); korrekt (Zeilen + NULL-Einzelsatz) → ok (T4). Jeweils
      Rollback-Transaktions-Test in
      `specs/migrations/20260808_ber122_trigger_tests.sql`.
- [x] Gegenrichtung: `belege.mwst_satz` neben existierenden Zeilen setzen → Fehler (T5).
- [x] Zeilen eines festgeschriebenen Belegs sind unveränderlich (INSERT/UPDATE/
      DELETE schlagen fehl — Testtrio T7–T9 wie `beleg_seiten`).
- [x] Kaskade beim Löschen eines offenen Belegs bleibt möglich (T10).
- [x] RLS: fremder Mandant sieht 0 Zeilen, eigener Mandant sieht seine (T11).
- [x] Bestand unberührt: keine Steuerzeilen an Nicht-Testbelegen (T12).
- [x] `docs/SCHEMA.md`, `ARCHITECTURE_DESIGN.md §9`, `INDEX.md` nachgezogen.

**Stufe 2–5 (Folge-Stories):**
- [ ] Freigabe eines 7/19-Restaurant-Testbelegs erzeugt zwei Steuerzeilen; Export
      enthält zwei Datenzeilen mit gleichem Belegfeld 1, korrekten Beträgen und
      Schlüsseln (`80`/`90` gemäß `steuerschluessel`-Konfiguration); Summe der
      Umsätze == Dokumentbrutto.
- [ ] Ein-Satz-Beleg erzeugt bit-identische Exportzeile wie vor der Story
      (Regressionstest gegen konservierte Fixture).
- [ ] Teilbetrag + Steuerzeilen → definierter 422.
- [ ] `docs/DATEV.md` (Buchungslogik: „eine Zeile pro Steuerzeile"), `docs/SCHEMA.md`,
      `docs/UEBERGABE.md` (v1-Beschränkung entfernen) nachgezogen.

## Technische Notizen

- Neue Dateien bei Umsetzung: Migration (Schwesterrepo),
  `src/components/steuerzeilen-editor.tsx`; geändert: `src/lib/datev.ts`
  (`belegRows`), Freigabe-Route, Detailseite, beide Export-Routen (JOIN Zeilen),
  n8n-Workflows (KI-Schema + INSERT) → §9/INDEX bei Umsetzung.
- Rundungsregel je Zeile: Beträge kommen erfasst (nicht berechnet) — keine
  Verteilungsrundung im Code; die Centgleichheit erzwingt der Konsistenz-Trigger.
- Reihenfolge bei Umsetzung: Migration → App → n8n (wie BER-120-Muster).

## Betroffene Komponenten

- [x] Frontend (Steuerzeilen-Editor)
- [x] Backend (Freigabe, Export)
- [x] Datenbank (eigene Migration)
- [x] n8n Workflow (KI-Schema, Insert)
- [ ] Edge Function

## Definition of Done

- [ ] Lokale Gates grün (ESLint, Semgrep, Typecheck)
- [ ] PR erstellt und gemerged
- [ ] Required Status Checks grün
- [ ] Kein "QA Failed" Label
- [ ] Spec-File aktualisiert (Status: done)
- [ ] Schema-Änderungen in `docs/SCHEMA.md` nachgezogen

## Status

`in progress` — Design-Fassung 23.07.2026, **Stufe 1 gebaut am 08.08.2026**
(Migration `20260808110034_beleg_steuerzeilen.sql` im Schwesterrepo +
Verhaltenstests). Stufen 2–5 offen als Folge-Stories. BER-117 hält die
Erweiterung nachweislich additiv offen.

## Human Review

- **Date:** 2026-08-08
- **Reviewer:** Operator (peerendees / kunkel@h-pcn.de)
- **Comment:** Freigabe der Schema-Änderung nach Vorlage des vollständigen SQL
  (Migration + Verhaltenstests). Geprüfte Punkte: GoBD-Festschreibungsschutz auf
  der Satellitentabelle (strenger als `beleg_seiten` — INSERT/UPDATE/DELETE
  gesperrt), Mandantenisolation ausschließlich über den Join auf `belege` ohne
  Selbstbezug (42P17-Regressionsschutz), Abweichung vom Entwurf hin zu
  `DEFERRABLE INITIALLY DEFERRED` samt Gegen-Trigger auf `belege`, und die
  Feststellung, dass die Migration am Bestand nichts ändert (Trigger feuern erst
  ab der ersten Steuerzeile; die Tabelle bleibt bis Stufe 2 leer).
- **Sensitive Paths Touched:** `supabase/migrations/**`
  (`threema-decrypt/supabase/migrations/20260808110034_beleg_steuerzeilen.sql`,
  gespiegelt als `specs/migrations/20260808_beleg_steuerzeilen.sql`)
- **Security-Review-Report:** n/a — kein `security-architect`-REVIEW-Lauf;
  Freigabe durch Operator-Review des vollständigen Diffs.
- **Offener Vorbehalt zum Zeitpunkt der Freigabe:** die SQL war noch nirgends
  ausgeführt (kein Syntax- oder Verhaltensnachweis). Der Nachweis erfolgt mit
  dem Lauf der Verhaltenstests.

## Session-Referenz

*(wird beim Commit ergänzt)*
